\chapter*{Summary of the thesis}
\addcontentsline{toc}{chapter}{Summary of the thesis}

\section*{Preface}
The evidence for dark matter is overwhelming, and yet it tells us almost nothing about what dark matter is made of. Galactic rotation curves stay flat where the visible mass predicts a decline; galaxies inside clusters move too fast to remain bound by the luminous matter alone; light from distant sources is bent by mass we cannot see; and the pattern of anisotropies in the cosmic microwave background is reproduced only when a non-luminous component is added to the budget. Each of these measurements, drawn from a different scale and a different epoch, points to the same conclusion: a form of matter that outweighs ordinary baryons by roughly a factor of five and interacts with light only through gravity. Within the standard cosmological model this component accounts for about 85\% of the matter density, cold and non-baryonic. The agreement across independent probes is remarkable, but it is also frustrating, because every one of these probes is gravitational. They tell us how much dark matter there is and where it sits; they say nothing about its particle nature. Proposed candidates span nearly ninety orders of magnitude in mass, from ultralight axions to primordial black holes, and the data alone single out none of them.

Among these candidates, weakly interacting massive particles (WIMPs) have long held a privileged place. A stable particle with an electroweak-scale mass and coupling, produced thermally in the hot early Universe, freezes out with a relic abundance that lands naturally near the observed dark matter density, a coincidence known as the WIMP miracle. The annihilation process that fixes this abundance does not switch off once the Universe cools: wherever dark matter piles up densely today, WIMPs should continue to annihilate, converting their rest mass into Standard Model particles such as gamma rays, neutrinos, positrons, and antiprotons. If dark matter is instead unstable, it may decay into the same states. Of the messengers this produces, gamma rays are the most informative. They travel undeflected by magnetic fields, so they point back to where they were made, and they carry the spectral fingerprint of the interaction that produced them. A gamma-ray signal from a region of concentrated dark matter would therefore be one of the cleanest handles we have on the particle behind the gravitational anomalies.

For more than fifteen years the Large Area Telescope (LAT) on board the Fermi Gamma-ray Space Telescope has surveyed the GeV sky where such signatures are expected, and its measurements sit at the core of this thesis. That long exposure has changed the character of the problem. The brightest targets have already been studied at length, and the signals that remain are faint, blended with astrophysical foregrounds, or hidden below the detection threshold. Standard methods classify the brightest objects and map the prominent diffuse components reliably, but they struggle with overlapping populations and with instrumental limits such as finite angular resolution and the detection threshold itself. Setting a threshold and counting what rises above it is a natural first move, yet it discards most of the information: a large fraction of the emitting population is left unresolved, unclassified, or simply unseen.

We argue in this thesis that progress in indirect dark matter searches now depends on statistical and machine-learning methods able to recover signals in these noise-dominated regimes, where simple thresholding fails. The work follows an arc from the Galactic Center outward to the cosmic web, tracking the expected signal into regimes of steadily lower signal-to-noise. Each regime calls for its own tools, and we draw in turn on traditional statistical inference, generative modeling, implicit-likelihood techniques, and deep learning, applied both to Fermi-LAT data and to forecasts for future instruments.

Five studies make up this progression. We begin with a measurement of the gamma-ray luminosity function of millisecond pulsars (MSPs) in Milky Way globular clusters, used to test whether an unresolved pulsar population can account for the Galactic Center Excess. We then search for dark matter subhalos among the unassociated Fermi-LAT sources, combining a generative mixture model with quantification learning. Moving below the catalog threshold, we reconstruct the source-count distribution of faint extragalactic sources with a deep neural network, and we build a probabilistic cataloging framework that extends gamma-ray catalogs into the sub-threshold regime, released publicly as the gamma-ray Photon Count Statistics (gPCS) catalog, a Python package accompanied by a FITS table. Finally, we forecast the sensitivity of the Cherenkov Telescope Array Observatory to the cross-correlation between gamma rays and galaxy catalogs, carrying the search out to the largest scales and the weakest expected signals.

\section*{The dark matter problem}
The case for dark matter rests on a chain of independent observations assembled over nearly a century. The first quantitative hint came in 1933, when Zwicky applied the virial theorem to the radial velocities of galaxies in the Coma Cluster and inferred a mass-to-light ratio of order 400, far larger than the luminous matter could supply; he named the missing component \emph{dunkle Materie} \cite{Zwicky:1933}. The discrepancy reappeared on galactic scales when Rubin and Ford measured the first high-precision optical rotation curve of M31 in 1970 and found no Keplerian decline at large radii \cite{Rubin:1970zza}. Later 21-cm observations extended these rotation curves to the largest measurable radii in hundreds of systems, where the rotation velocity stays flat; a flat curve implies an enclosed mass growing linearly with radius, $M(r) \propto r$, and therefore an extended halo whose density falls as $\rho \propto r^{-2}$ at large radii. Gravitational lensing supplied a cleaner argument still: in the Bullet Cluster the weak-lensing mass reconstruction is spatially offset from the X-ray-emitting gas and coincides instead with the effectively collisionless galaxies \cite{Clowe:2006eq}, as expected if most of the mass is dark and nearly collisionless. On cosmological scales the cosmic microwave background completes the picture, since a universe without cold dark matter would produce a strongly suppressed third acoustic peak, in conflict with the observed spectrum.

These measurements agree not only that dark matter exists but on how much of it there is. The Planck 2018 analysis yields $\Omega_c h^2 = 0.1200 \pm 0.0012$ for the cold dark matter density \cite{Aghanim:2018eyx}, making dark matter roughly five times more abundant than ordinary baryonic matter. Any viable candidate must reproduce this relic density while remaining consistent with the dynamical and lensing evidence.

A WIMP offers a natural way to do so: a particle with a mass in the GeV--TeV range and weak-scale couplings to the Standard Model. If such a particle was in thermal equilibrium in the hot early universe and froze out as the plasma cooled, its surviving abundance is set by the rate at which it annihilated. For $s$-wave annihilation this relic abundance takes the compact form
\be
\frac{\Omega_\chi h^2}{0.12} \approx \frac{2.2 \times 10^{-26}~\mathrm{cm^3/s}}{\langle \sigma v_\mathrm{rel}\rangle},
\ee
where the numerator is the canonical thermal cross section, $\langle \sigma v\rangle \approx 2.2 \times 10^{-26}~\mathrm{cm^3/s}$ \cite{Steigman:2012nb, Cirelli:2024ssz}. What makes the hypothesis compelling is a coincidence: a cross section of roughly weak strength, $\langle \sigma v\rangle \sim \alpha^2/m_\chi^2$ with $\alpha \sim 0.03$, returns the observed abundance for a mass of a few hundred GeV to a few TeV, precisely the electroweak scale. This alignment between a cosmological measurement and a particle-physics scale is the so-called WIMP miracle.

The same coupling that governs dark matter annihilation in the early universe also mediates scattering off nuclei in underground detectors and pair-production at particle colliders, which opens three complementary experimental strategies. Direct detection searches for the nuclear recoil imparted when a dark matter particle crosses a deep-underground detector. Collider searches look for the missing transverse energy carried away when dark matter is produced at the Large Hadron Collider. Indirect detection, the approach followed throughout this thesis, targets the annihilation or decay products of dark matter in astrophysical environments where its density is high.

For annihilation into gamma rays, the observed differential flux factorizes cleanly into a particle-physics term and an astrophysical term,
\be
\frac{d\Phi_\gamma}{dE\, d\Omega} = \underbrace{\frac{1}{4\pi} \frac{\langle \sigma v_\text{rel} \rangle}{2 m_\chi^2} \frac{dN_\gamma}{dE}}_{\text{Particle Physics}} \quad \times \underbrace{J(\psi)}_{\text{Astrophysics}},
\ee
where the astrophysical factor is the line-of-sight integral of the squared density,
\be
J(\psi) = \int_\text{l.o.s.} \rho^2\bigl(r(s, \psi)\bigr) \, ds.
\ee
The particle-physics factor holds the mass $m_\chi$, the cross section $\langle \sigma v_\mathrm{rel}\rangle$ (or, for decay, the rate $\Gamma$), and the spectral shape $dN_\gamma/dE$: it encodes the model we wish to test \cite{Cirelli:2024ssz, Hooper:2024}. The astrophysical factor $J$ encodes only the distribution of dark matter along the line of sight. This separation is what makes indirect searches tractable, because a non-detection in a given direction translates directly into an upper limit on $\langle \sigma v_\mathrm{rel}\rangle$ once $J$ is known. It also fixes where the leading uncertainty lies: $J$ is quadratic in the density profile, and the profile is the quantity we measure least well, so the astrophysics dominates the systematic budget of any annihilation search. (For decaying dark matter the flux instead scales with $\Gamma/m_\chi$ and a $D$-factor linear in $\rho$, which softens this sensitivity to the profile.)

Because the expected signal scales with the density integral, $J$ orders the targets of any annihilation search, and with them the structure of this thesis. The Galactic Center has the largest J-factor of any astronomical source, owing to its proximity ($\sim 8$~kpc) and the steep expected rise of the dark matter density at small radii; it also carries the most severe astrophysical contamination, from bright diffuse emission and a crowded field of point sources. Dwarf spheroidal galaxies and dark subhalos sit lower in $J$ but far higher in cleanliness: dwarfs are among the most dark-matter-dominated systems known, and $\Lambda$CDM predicts many more gravitationally bound subhalos than the roughly 50 known satellite galaxies, most of them entirely dark and expected to appear in the Fermi-LAT data as unassociated point sources with no counterpart at other wavelengths \cite{Springel:2008cc}. At the largest scales lies the extragalactic web, whose dark matter contribution is buried in the unresolved gamma-ray background that remains once resolved sources and Galactic diffuse emission are subtracted; there the signal is expected to be subdominant in total intensity and must be extracted with anisotropy and cross-correlation techniques. The chapters that follow descend this hierarchy in order: the Galactic Center first, where the expected signal is brightest, then dark subhalos, then the faint sub-threshold source population, and finally the cosmic web, at the weakest signal of all.

\section*{The gamma-ray sky and the \textit{Fermi} Large Area Telescope}
Every target in this hierarchy is seen through the same instrument. Launched in 2008, the Fermi Large Area Telescope has provided the most sensitive all-sky survey of the GeV domain to date, operating continuously for over fifteen years. It is a pair-conversion telescope: an incident gamma ray converts into an electron-positron pair, and four subsystems track the resulting charged particles, measure their energy, and reject the overwhelming cosmic-ray background~\cite{Atwood:2009ez}. The instrument is sensitive from 20~MeV to over 300~GeV, and with a field of view of roughly 2.4~sr at 1~GeV it images nearly the entire sky with uniform exposure every two orbits, about every three hours. Its angular resolution improves sharply with energy: the 68\% containment radius of the point-spread function (PSF) falls from several degrees near 100~MeV to about $0.6^\circ$ at 1~GeV and below $0.15^\circ$ above 10~GeV~\cite{Atwood:2009ez}. This broad low-energy PSF blurs neighboring sources together, which limits how deep any catalog can reach and, as we develop throughout this thesis, motivates statistical methods that work with the population rather than the individual source.

The GeV sky that emerges is naturally decomposed into a few components. The observed emission is dominated by the Galactic diffuse foreground, produced when cosmic rays interact with interstellar gas and radiation fields; its template-based modeling remains the largest source of systematic uncertainty in Fermi-LAT analyses. Superimposed on this foreground is a population of resolved point sources, most of them extragalactic blazars, together with Galactic sources of which pulsars are the most prominent. What remains once the Galactic diffuse emission and every cataloged source are subtracted is an almost isotropic residual, the unresolved gamma-ray background (UGRB): the cumulative light of all sources still too faint to be individually detected, plus any genuinely diffuse isotropic component~\cite{Fermi-LAT:2014ryh}.

The boundary between ``resolved'' and ``unresolved'' is set by a detection threshold. The most recent incremental catalog release, 4FGL-DR4, analyzes 14 years of data and contains 7194 sources~\cite{Ballet:2023qzs}. A candidate enters the catalog only if its test statistic (TS), which compares the maximum likelihood of the data with and without the source, exceeds 25, a significance of just over $4\sigma$; this threshold has held fixed across the catalog's history. Everything below it is not absent from the data, but simply folded into the diffuse background rather than listed as a source.

Two blind spots in this picture motivate the chapters that follow. First, of the 4FGL-DR4 sources, 4021 are associated with extragalactic counterparts and 593 with Galactic ones, but 2577 remain unassociated, with no plausible counterpart at any other wavelength~\cite{Ballet:2023qzs}. Beyond its likely majority of faint blazars and pulsars, this unassociated population is the primary search pool for exotic emitters such as dark matter subhalos. Second, and by construction, the sources below threshold are measured only in aggregate: integrating the source-count distribution from zero flux up to the detection threshold reproduces the intensity of the UGRB, yet recovering the population that produces it requires techniques that read population-level information off the collective imprint of unresolved sources on the photon-count map. These two gaps, the unassociated and the sub-threshold, set the agenda for the methods developed in the remainder of this thesis.

\section*{Objectives}
These two blind spots set the objectives of this thesis. Our first objective is to test the millisecond-pulsar interpretation of the Galactic Center Excess by measuring the gamma-ray luminosity function of pulsars in the Milky Way's globular cluster system, a measurement that further sharpens the tension with the MSP interpretation as it has been formulated so far. A second goal is to ask whether any of the point sources detected by Fermi-LAT, yet left without a counterpart, could be dark matter subhalos, a question in which the shift between the associated population available for training and the unassociated one under study calls for a quantification learning framework. We then turn to the population below the detection threshold, reconstructing the source-count distribution $dN/dS$ of extragalactic sources with a deep learning approach that recovers it down to a factor of 50 fainter than the Fermi-LAT threshold, and extending the catalogs probabilistically by comparing the real sky against ensembles of synthetic skies, so that population-level knowledge is complemented with positional identifications. A further objective is to isolate a dark matter signal hidden in the UGRB and to forecast the reach of the Cherenkov Telescope Array Observatory (CTAO) for annihilation and decay. Finally, across all five, we develop the statistical and machine-learning methodology that makes faint signals accessible in noise-dominated regimes and that transfers to future instruments.

\section*{Methodology: statistics and machine learning for faint signals}
\textbf{Bayesian inference in the noise-dominated regime.} A dark matter signal in the gamma-ray sky is faint by construction: it sits beneath bright astrophysical backgrounds whose normalization and morphology are themselves uncertain. To extract it we must infer model parameters from a binned photon-count map, and the standard tool is a pixel-wise Poisson likelihood,
\be
\mathcal{L}(\boldsymbol{\theta}) = \prod_{a=1}^{N_{\mathrm{pix}}} \frac{\lambda_a(\boldsymbol{\theta})^{k_a} \, e^{-\lambda_a(\boldsymbol{\theta})}}{k_a!},
\ee
where $k_a$ is the observed count and $\lambda_a(\boldsymbol{\theta})$ the model expectation in pixel $a$. This construction works only while the expectation can be written in closed form. It breaks down when the data-generating process involves a complex simulator whose internal stochastic structure prevents the likelihood from being written explicitly, because evaluating it then requires marginalizing over a set of latent variables $\mathbf{z}$,
\be
p(\mathbf{x}|\boldsymbol{\theta}) = \int p(\mathbf{x}, \mathbf{z}|\boldsymbol{\theta}) \, d\mathbf{z}.
\ee
The unresolved point-source problem is the canonical example: writing a closed-form likelihood for the full sky map would require integrating over every possible spatial configuration, flux, and number of the unresolved sources, an integral no one can perform. This is the inference problem that the population and catalog analyses summarized below are built to solve.

\textbf{Simulation-based inference.} When the likelihood is intractable but the forward model is easy to sample, we can sidestep the integral entirely and learn from simulations instead. This is the idea behind \emph{simulation-based inference}~\cite{Cranmer:2020}: we generate many mock sky maps from known parameters and train a neural network to invert the map. In the neural posterior estimation (NPE) variant we adopt, a conditional density estimator, typically a normalizing flow, is trained to approximate the posterior directly, $q_\phi(\boldsymbol{\theta}|\mathbf{x}) \approx p(\boldsymbol{\theta}|\mathbf{x})$. The payoff is that the inference is \emph{amortized}: once the network is trained, applying it to the real Fermi-LAT sky map costs almost nothing, since the expensive simulation and training happen only once. In our implementation the network maps an observed sky map directly to posterior summaries, the mean and variance for each flux bin. This estimator is the engine behind the source-count measurement on the Fermi-LAT sky summarized below.

\textbf{Quantification learning.} Often the physical question is not what each object is but how many of each kind there are: the fraction of unassociated point sources that are millisecond pulsars, for instance, directly controls how much residual signal is left for dark matter. Machine-learning classifiers are typically trained on associated sources and then applied to unassociated ones, but several observational biases break the assumption that the two populations follow the same distribution. \emph{Quantification learning} addresses this by fitting the mixture weights $\pi_k$ of a generative model, $p(\mathbf{x}) = \sum_k \pi_k\, p(\mathbf{x}|k)$, directly to the target data rather than relying on the class fractions of the training set~\cite{10.1145/3117807_Gonzalez_quantification}. Two failure modes must be separated. Under \emph{prior shift} the class-conditional densities agree, $p_{\mathrm{train}}(\mathbf{x}|k) = p_{\mathrm{target}}(\mathbf{x}|k)$, while the class proportions differ, $p_{\mathrm{train}}(k) \neq p_{\mathrm{target}}(k)$; we correct it by fitting the $\pi_k$ to the unassociated data. Under \emph{covariate shift} the posteriors agree, $p_{\mathrm{train}}(k|\mathbf{x}) = p_{\mathrm{target}}(k|\mathbf{x})$, while the feature distributions differ, $p_{\mathrm{train}}(\mathbf{x}) \neq p_{\mathrm{target}}(\mathbf{x})$; we correct it with sigmoid modulation functions. Any analysis that ignores this mismatch risks biased prevalence estimates and, in the context of dark matter searches, unreliable upper bounds on the annihilation cross-section. This correction underlies the source-classification limits summarized below.

\textbf{Deep learning on sky maps.} The information we need is encoded in the morphology of the emission, not in any single pixel, and a gamma-ray map is naturally a two-dimensional grid or a HEALPix tessellation of the sphere. Convolutional neural networks (CNNs) exploit this spatial structure directly. Stacking convolutional layers builds progressively larger receptive fields: shallow layers detect local features such as edges and peaks, while deeper layers combine these into more abstract representations of the underlying source population. In this thesis a sky map is fed into such a convolutional network to reconstruct the source-count distribution, providing the feature extractor that turns the raw Fermi-LAT map into the summaries the inference reads. This is the network behind the source-count reconstruction summarized below.

\textbf{Probabilistic cataloging.} A conventional source catalog applies a hard detection threshold, keeping only directions whose test statistic exceeds a fixed cut and discarding everything fainter, even though the sub-threshold population carries most of the information about the faint end of the source-count distribution~\cite{Mattox:1996zz}. \emph{Probabilistic cataloging} replaces the hard cut with a likelihood: each sky direction is assigned a probability of hosting a sub-threshold source. In practice we perform a frequentist comparison of the real sky against ensembles of synthetic skies generated from the recovered source-count distribution, flagging candidate source directions below the detection threshold rather than declaring them present or absent. This machinery underlies the probabilistic source catalog summarized below.

\textbf{Cross-correlations.} A pure dark matter signal in the diffuse gamma-ray background is hopeless to isolate map by map, because it is buried under astrophysical emission and instrumental noise. The way out is to correlate the gamma-ray sky with an independent tracer of the same underlying matter distribution. The relevant statistic is the angular power spectrum (APS), whose two-map generalization reads
\be
C_\ell^{ij} = \frac{1}{2\ell+1} \left\langle \sum_{m=-\ell}^{\ell} a_{\ell m}^{(i)}\, a_{\ell m}^{(j)*} \right\rangle,
\ee
where the $a_{\ell m}^{(i)}$ are the harmonic coefficients of map $i$. When $i \neq j$ we obtain the cross-correlation APS, and because the noise terms of two different maps are uncorrelated they drop out of the average, leaving the shared cosmological signal. We use this to forecast the sensitivity of CTAO to dark matter annihilation and decay, cross-correlating the predicted gamma-ray emission with the 2MASS galaxy catalog~\cite{Camera:2012cj}. This forecast is the last of the results summarized below.

\section*{Results}
The results of the thesis unfold along its guiding arc, moving from the brightest and most statistically compelling signals toward progressively fainter ones, and with each step the methodology grows more sophisticated as the signal recedes.

\textbf{The millisecond-pulsar luminosity function and the Galactic Center Excess.} The Galactic Center Excess (GCE) is a bright, statistically significant excess of GeV emission from the region surrounding the Galactic Center, with a spectrum and morphology consistent with the annihilation of a dark matter particle near the thermal relic value~\cite{Goodenough:2009gk, Hooper:2010mq, Daylan:2014rsa}. Its leading astrophysical interpretation attributes the signal instead to an unresolved population of centrally located MSPs, individually too faint to be resolved. To discriminate between these hypotheses, we characterized the MSP luminosity function where it can be measured directly, in the Milky Way's globular clusters. Using 15.8 years of publicly available Fermi-LAT data, we analyzed 157 globular clusters and reported the robust detection of 56 of them with a test statistic $\mathrm{TS} > 25$~\cite{Amerio:2024qor}; eight of these detections are absent from previous gamma-ray source catalogs.

We then fit the luminosity function on the 87 clusters with high stellar encounter rates and/or well-measured luminosities, adopting a log-normal form and constructing a marginalized joint likelihood over all clusters that accounts for their distances and the Poisson-distributed number of pulsars each hosts. This hierarchical Bayesian treatment yielded a mean pulsar luminosity $\langle L_\gamma\rangle \sim (1\text{--}8)\times 10^{33}$ erg/s, integrated between 0.1 and 100 GeV, and a width $\sigma_L \sim 1.4\text{--}2.7$. Taken together, these 87 clusters host of order 400--1500 gamma-ray emitting MSPs.

Armed with this luminosity function, we asked what it implies for the GCE. If the excess were generated by MSPs sharing the characteristics of those in globular clusters, Fermi's source catalogs would already contain $N_{\rm MSP} \sim 17\text{--}37$ individual members of such an Inner Galaxy population. Yet only three MSPs have been detected with a direction and distance that do not preclude membership in that population~\cite{Holst:2024fvb}. A more conservative model that allows for cluster-to-cluster variation lowers the expected count to $N_{\rm MSP} \approx 3\text{--}30$, but the results nonetheless remain in tension with the three identified candidates at a level of at least $2\sigma$.

The luminosity function we derived is therefore incompatible with pulsar interpretations of the GCE, unless a large number of unassociated Fermi sources are in fact unidentified MSPs. Given the systematic efforts already devoted to searching for radio pulsations from unassociated Fermi sources, this possibility seems unlikely. This result poses a challenge to pulsar interpretations of the excess and, by extension, potentially bolsters the case for a dark matter origin of this long-standing signal.

\textbf{Dark matter subhalos among the unassociated sources.} $\Lambda$CDM predicts a large population of dark subhalos surrounding the Milky Way~\cite{Springel:2008cc}, the densest of which could annihilate at a visible rate and appear as point sources lacking any multi-wavelength counterpart. These unassociated sources have long been recognized as the natural testing ground for DM subhalo searches, yet to date no unassociated Fermi-LAT source has been unambiguously confirmed as a dark matter subhalo. We revisited the question using the latest incremental release of the Fermi catalog, 4FGL-DR4~\cite{Ballet:2023qzs}, which contains 7,195 sources, of which 2,563 -- roughly one third -- remain unassociated. Among these, 1,283 lie at Galactic latitudes $|b| > 10^\circ$, and we restricted the analysis to that sample.

We described the unassociated population with a probabilistic model $p(x)$ built as a generative mixture model of three source classes: Galactic and extragalactic astrophysical sources, plus the hypothetical DM subhalos~\cite{Amerio:2025fhz}. Each class is characterized through its log-parabola spectral features, namely flux, spectral slope, and curvature. To account for the mismatch between the labeled training sample and the unassociated data, we worked within the general framework of quantification learning, considering two effects: a covariate shift affecting all source classes in the same way, modeled through sigmoid modulation functions, and a prior probability shift, which allows the class prevalences to change and admits the possible presence of a new source class. We fit the model with a semi-supervised expectation-maximization algorithm. The data prefer the prior shift at $7\sigma$ and the covariate shift at $4.5\sigma$, with a combined significance of $6.7\sigma$, confirming that both effects are present.

No significant excess corresponding to DM subhalos was observed beyond the Galactic and extragalactic populations; for all tested models the significance is less than $2\sigma$. We therefore derived 95\% confidence upper bounds on the DM annihilation cross section $\langle \sigma v\rangle$ in the $b\bar{b}$ channel, for dark matter masses between 10 GeV and 1 TeV. These limits are about an order of magnitude less stringent than those obtained from dwarf spheroidal galaxies, but they are competitive with previous limits derived from DM subhalo searches using classify-and-count methods.
% <<RESULTS-DNDS>>
% <<RESULTS-CATALOG>>
% <<RESULTS-XCORR>>

\section*{Conclusions and future directions}
% <<CONCLUSIONS>>

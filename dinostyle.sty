%% mythesis.sty
%% PhD Thesis Style File — Aurelio Amerio
%%
%% Consolidates all package loading, layout settings, chapter styles,
%% page styles, math commands and thesis-specific macros so that main.tex
%% can stay clean and minimal.
%%
\NeedsTeXFormat{LaTeX2e}
\ProvidesPackage{dinostyle}[2026/03/04 v1.0 PhD Thesis Style]

% ============================================================
% 1. ENCODING & LANGUAGE
% ============================================================
\RequirePackage[utf8]{inputenc}
\RequirePackage[T1]{fontenc}
% spanish loaded for the resumen, catalan for a future valencian resum;
% english last = document default.
% es-noshorthands keeps spanish babel from activating special characters
% (quoting/decimal shorthands) that clash with math and existing macros.
% (babel has no separate valencian option: hyph-ca covers both standards)
\RequirePackage[spanish,es-noshorthands,catalan,english]{babel}
% \RequirePackage[none]{hyphenat}          % disable hyphenation
\RequirePackage{hyphenat}     


% some hyphenations not working by default:
\hyphenation{brems-strah-lung}

% ============================================================
% 2. FONT
% ============================================================
% New Century Schoolbook with math support (fouriernc pulls in fourier internally)
\RequirePackage{fouriernc}
\RequirePackage{microtype}               % improved pdf typography

% ============================================================
% 3. MATH PACKAGES
% ============================================================
\RequirePackage{amsmath}
\RequirePackage{amsfonts}
\RequirePackage{amssymb}
\RequirePackage{amsthm}
\RequirePackage{mathtools}
\RequirePackage{mathrsfs}               % script math font (\mathscr for Lagrangians etc.)
\RequirePackage{bigints}
\newcommand\hmmax{0}
\newcommand\bmmax{0}                    % prevent "too many math alphabets"
\RequirePackage{bm}
\DeclareMathAlphabet{\mathcal}{OMS}{cmsy}{m}{n}

% ============================================================
% 4. GRAPHICS & FIGURES
% ============================================================
\RequirePackage{graphicx}
\RequirePackage{float}
\RequirePackage{longtable,rotating}
\RequirePackage[dvipsnames]{xcolor}      % extend color (dvipsnames for papers)
\RequirePackage{tikz}                   % also loads pgf; used by \pgftextcircled
\usetikzlibrary{arrows,shapes,automata,backgrounds,petri,topaths}

% ============================================================
% 5. CAPTIONS & SUBCAPTIONS
% ============================================================
\RequirePackage[labelfont=bf]{caption}
\captionsetup{labelfont=bf}
\captionsetup[figure]{labelfont={bf}, name={Fig.}, labelsep=period}
\captionsetup[table]{labelfont={bf}, name={Table}, labelsep=period,
                     justification=raggedright, singlelinecheck=false}
\RequirePackage{subcaption}

% ============================================================
% 6. TABLES & VERBATIM
% ============================================================
\RequirePackage{booktabs}                % professional-looking tables (papers)
\RequirePackage{verbatim}
\RequirePackage{comment}                 % comment environment (papers)

% ============================================================
% 7. BIBLIOGRAPHY
% ============================================================
\RequirePackage[square,numbers,sort&compress]{natbib}

% ============================================================
% 8. HYPERREF & URLS  (load near last)
% ============================================================
% Thesis metadata — override these in main.tex before \begin{document}
\newcommand{\thesisauthor}{Author Name}
\newcommand{\thesistitle}{Thesis Title}
\newcommand{\thesisadvisorone}{Advisor One}
\newcommand{\thesisadvisortwo}{Advisor Two}

% Author annotation marker — available document-wide (incl. frontmatter),
% since macros.tex is only loaded in \mainmatter. macros.tex re-provides it
% for standalone paper builds that do not load dinostyle.
\providecommand{\aure}[1]{\textcolor{orange}{[AA: #1]}}

\RequirePackage{url}
\RequirePackage[hidelinks,
                colorlinks=true,
                urlcolor=black,
                linkcolor=black,
                citecolor=black]{hyperref}
\RequirePackage{memhfixc}               % memoir + hyperref fix (after hyperref)
\RequirePackage[nameinlink,capitalise]{cleveref}  % \cref, \Cref — must load after hyperref

% Pass metadata to hyperref after the user has set \thesisauthor / \thesistitle
\AtBeginDocument{%
  \hypersetup{pdfauthor={\thesisauthor}, pdftitle={\thesistitle}}%
}

% ============================================================
% 9. MISC UTILITIES
% ============================================================
\RequirePackage{import}                 % relative \includegraphics paths
\RequirePackage{ifpdf}
\RequirePackage{ifthen}                 % \ifthenelse used for \renderpapers flag
\RequirePackage{pdfpages}
\RequirePackage{calc,soul}
\RequirePackage{lettrine}               % drop-cap initials used in chapters
\RequirePackage{enumitem}               % enumerate/itemize customisation (papers)
\RequirePackage{listings}               % code listings (paper 3)
\RequirePackage[normalem]{ulem}         % strikethrough \st (papers)
\RequirePackage{appendix}               % subappendices environment (papers)
% Note: titlesec is NOT used — memoir handles sectioning natively

% ============================================================
% 10. GLOSSARIES & ACRONYMS
% ============================================================
\RequirePackage[acronym,toc]{glossaries}
\RequirePackage{glossary-mcols}
\glsdisablehyper

% Convenience shortcuts
\newcommand{\as}{\acrshort}
\newcommand{\al}{\acrlong}
\newcommand{\af}{\acrfull}

% ============================================================
% 11. MEMOIR PAGE LAYOUT  (memoir-specific)
% ============================================================
\settrimmedsize{297mm}{210mm}{*}
\setlength{\trimtop}{0pt}
\setlength{\trimedge}{\stockwidth}
\addtolength{\trimedge}{-\paperwidth}
\settypeblocksize{634pt}{448.13pt}{*}
\setulmargins{4cm}{*}{*}
\setlrmargins{*}{*}{1.5}
\setmarginnotes{17pt}{51pt}{\onelineskip}
\setheadfoot{\onelineskip}{2\onelineskip}
\setheaderspaces{*}{2\onelineskip}{*}
\checkandfixthelayout

% Binding-friendly margins
\setlength{\oddsidemargin}{0.55cm}
\setlength{\evensidemargin}{-0.55cm}

% ============================================================
% 12. TYPOGRAPHY & SPACING  (ported from jcappub.sty)
% ============================================================

% Line spacing: JCAP uses default single spacing
\renewcommand{\baselinestretch}{}
\setlength\lineskip{1\p@}
\setlength\normallineskip{1\p@}

% Paragraph
\frenchspacing
\setlength\parskip{0\p@ \@plus \p@}
\AtBeginDocument{\setlength\parindent{1.2\parindent}}

% Break penalties (matches JCAP exactly)
\@lowpenalty   51
\@medpenalty  151
\@highpenalty 301
\widowpenalty 1000
\clubpenalty  1000

% Floats
\setcounter{topnumber}{4}
\renewcommand\topfraction{1}
\setcounter{bottomnumber}{1}
\renewcommand\bottomfraction{.6}
\setcounter{totalnumber}{5}
\renewcommand\textfraction{0}
\renewcommand\floatpagefraction{0.8}
\setlength{\floatsep}{14pt plus 8pt minus 6pt}

% Array / table spacing
\setlength\arraycolsep{2\p@}
\setlength\tabcolsep{6\p@}
\setlength\arrayrulewidth{.4\p@}
\setlength\doublerulesep{2\p@}
\setlength\tabbingsep{\labelsep}
\skip\@mpfootins = \skip\footins
% Footnotes: enlarge the per-page space the footnote area may occupy, and
% strongly discourage splitting a single footnote across a page break.
\dimen\footins=0.8\textheight            % extend the footer space budget
\interfootnotelinepenalty=10000          % keep one footnote on a single page
\setlength\fboxsep{3\p@}
\setlength\fboxrule{.4\p@}

% Equation numbering: section.equation (JCAP style)
\numberwithin{equation}{section}

% No leader dots in the TOC (JCAP style)
\renewcommand{\@dotsep}{10000}

% Memoir sectioning depth
\setsecnumdepth{subsection}
\maxsecnumdepth{subsubsection}

% Memoir float sub-environments
\newsubfloat{figure}
\newsubfloat{table}

% ============================================================
% 13. CHAPTER STYLE  (Daleif modified)
% ============================================================
\makeatletter
\newlength\dlf@normtxtw
\setlength\dlf@normtxtw{\textwidth}
\newsavebox{\feline@chapter}
\newcommand\feline@chapter@marker[1][4cm]{%
  \sbox\feline@chapter{%
    \resizebox{!}{#1}{\fboxsep=1pt%
      \colorbox{gray}{\color{white}\thechapter}%
    }}%
  \rotatebox{90}{%
    \resizebox{%
      \heightof{\usebox{\feline@chapter}}+\depthof{\usebox{\feline@chapter}}}%
    {!}{\scshape\so\@chapapp}}\quad%
  \raisebox{\depthof{\usebox{\feline@chapter}}}{\usebox{\feline@chapter}}%
}
\newcommand\feline@chm[1][4cm]{%
  \sbox\feline@chapter{\feline@chapter@marker[#1]}%
  \makebox[0pt][c]{% i.e. \rlap
    \makebox[1cm][r]{\usebox\feline@chapter}%
  }}
\makechapterstyle{daleifmodif}{%
  \renewcommand\chapnamefont{\normalfont\Large\scshape\raggedleft\so}
  \renewcommand\chaptitlefont{\normalfont\Large\bfseries\scshape}
  \renewcommand\chapternamenum{}\renewcommand\printchaptername{}
  \renewcommand\printchapternum{\null\hfill\feline@chm[2.5cm]\par}
  \renewcommand\afterchapternum{\par\vskip\midchapskip}
  \renewcommand\printchaptertitle[1]{\color{gray}\chaptitlefont\raggedleft ##1\par}
}
\makeatother
\chapterstyle{daleifmodif}

% ============================================================
% 13b. SECTION HEADING STYLE  (ported from jcappub.sty)
% ============================================================
% memoir uses \setsecheadstyle etc., but since we need the same
% look as JCAP we patch via \@startsection directly.
\makeatletter
\renewcommand\section{\@startsection{section}{1}{\z@}%
  {-3.5ex \@plus -1.3ex \@minus -.7ex}%
  {2.3ex \@plus.4ex \@minus .4ex}%
  {\normalfont\large\bfseries}}
\renewcommand\subsection{\@startsection{subsection}{2}{\z@}%
  {-2.3ex \@plus -1ex \@minus -.5ex}%
  {1.2ex \@plus .3ex \@minus .3ex}%
  {\normalfont\normalsize\bfseries}}
\renewcommand\subsubsection{\@startsection{subsubsection}{3}{\z@}%
  {-2.3ex \@plus -1ex \@minus -.5ex}%
  {1ex \@plus .2ex \@minus .2ex}%
  {\normalfont\normalsize\bfseries}}
\renewcommand\paragraph{\@startsection{paragraph}{4}{\z@}%
  {1.75ex \@plus 1ex \@minus .2ex}%
  {-1em}%
  {\normalfont\normalsize\bfseries}}
\renewcommand\subparagraph{\@startsection{subparagraph}{5}{\z@}%
  {1.75ex \@plus 1ex \@minus .2ex}%
  {-1em}%
  {\normalfont\normalsize\itshape}}
\makeatother

% ============================================================
% 14. PAGE STYLE
% ============================================================
\makepagestyle{myvf}
% Headers already use \textsc; uppercasing marks would defeat the small caps
% and breaks on titles containing \textcolor (color names get uppercased too).
\nouppercaseheads
\makeoddfoot{myvf}{}{\thepage}{}
\makeevenfoot{myvf}{}{\thepage}{}
\makeheadrule{myvf}{\textwidth}{\normalrulethickness}
\makeevenhead{myvf}{\small\textsc{\leftmark}}{}{}
\makeoddhead{myvf}{}{}{\small\textsc{\rightmark}}
\pagestyle{myvf}

% Utility: fills blank pages to next odd-numbered page
\newcommand{\clearemptydoublepage}{%
  \newpage{\thispagestyle{empty}\cleardoublepage}}

% Index
\makeindex

% ============================================================
% 15. LETTRINE (decorative drop-cap)
% ============================================================
\newcommand{\initial}[1]{%
  \lettrine[lines=3,lhang=0.33,nindent=0em]{%
    \color{gray}{\textsc{#1}}}{}}







% ============================================================
% 19. IMPROVED SQUARE ROOT
% ============================================================
\let\oldsqrt\sqrt
\def\sqrt{\mathpalette\DHLhksqrt}
\def\DHLhksqrt#1#2{%
  \setbox0=\hbox{$#1\oldsqrt{#2\,}$}\dimen0=\ht0
  \advance\dimen0-0.2\ht0
  \setbox2=\hbox{\vrule height\ht0 depth -\dimen0}%
  {\box0\lower0.4pt\box2}}



% Draft watermark (only if \ifdraftdoc is true — requires memoir)
\ifdraftdoc
  \RequirePackage{draftwatermark}
  \SetWatermarkScale{0.3}
  \SetWatermarkText{\bf Draft: \today}
\fi
